import React from 'react';
import img from '../error.png';

const ErrorMessage = () => {
    return (
        <>
            <img src={img} slt="error"></img>
        </>
    )
}

export default ErrorMessage;